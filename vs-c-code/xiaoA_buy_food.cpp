#include<iostream>
#include<vector>
//#include<string>

using namespace std;

int main() {
	int n, k;
	cin >> n >> k;
	vector<int>num(n, 0);
	for (int i = 0; i < n; i++) {
		cin >> num[i];
	}

	return 0;
}